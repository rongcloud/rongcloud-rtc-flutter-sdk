import 'rcrtc_output_stream.dart';

class RCRTCAudioOutputStream extends RCRTCOutputStream {
  static const String _tag = 'RCRTCAudioOutputStream';

  RCRTCAudioOutputStream.fromJson(stream) : super.fromJson(stream);
}
